ANTHROPIC_API_KEY=
OPENAI_API_KEY=
GEMINI_API_KEY=
XAI_API_KEY=

# Amazon Bedrock: API key (bearer token) + region for the bedrock-runtime endpoint.
# Region is required — BEDROCK_REGION takes priority, otherwise AWS_REGION is used.
BEDROCK=
BEDROCK_REGION=
